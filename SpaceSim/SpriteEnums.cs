namespace SpaceSim
{
    public enum Direction4
    {
        Down,
        Left,
        Up,
        Right
    }

    public enum PlayerAnimation
    {
        Idle,
        Walking,
    }

    public enum DefaultAnimation
    {
        Default
    }

}

